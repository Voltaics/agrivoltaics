version: "3.9"
services:
  api:
    build: .
    ports:
      - "8000:8000"
    # Model is loaded from backend/model inside the image (see backend/main.py).
    # Mount your host model folder over that path if you want to swap weights without rebuilding:
    # volumes:
    #   - ./model:/app/backend/model
